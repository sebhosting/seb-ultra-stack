title: "SEB Ultra Stack Documentation"
description: "⚡ The Ultimate WordPress Multisite + WooCommerce Stack - Bad to the Bone Documentation"
baseurl: "/seb-ultra-stack"
url: "https://sebhosting.github.io"

# Theme
theme: jekyll-theme-midnight

# Custom variables
author: "SEB Hosting"
github_username: sebhosting
repository: seb-ultra-stack

# Enable syntax highlighting
highlighter: rouge
markdown: kramdown

# Plugins
plugins:
  - jekyll-sitemap
  - jekyll-feed
  - jekyll-sass-converter  # Ensure Sass to CSS conversion

# Language
lang: en

# Sass Settings (optional if needed)
sass:
  style: compressed  # You can change this to 'expanded' for easier debugging
  load_paths:
    - "assets/css"   # Make sure Jekyll knows where to find the Sass files

# Enable auto-generated sitemap
sitemap:
  exclude:
    - /assets/
    - /test/

# Additional settings
exclude:
  - README.md
  - LICENSE
  - .git
  - .github
